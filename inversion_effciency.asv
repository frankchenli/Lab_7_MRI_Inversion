clear all;
close all;
addpath ./data/
filenames = dir(fullfile('./data', '*.IMA'));
filenames2 = dir(fullfile('./data', '*.AS'));


for i = 1:numel(filenames)
    data(i).ima = dicomread(filenames(i).name);
    data(i).sr = 
end


for i = 1:16:size(data,2)
    figure;
    for j = 1:16
        subplot(4,4,j);
        imshow(data(j+i-1).matrix, []);
    end
end


